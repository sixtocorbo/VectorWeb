@page "/configuracion/rangos"
@using Microsoft.AspNetCore.Authorization
@attribute [Authorize(Policy = "configuracion.rangos")]
@using VectorWeb.Models
@using VectorWeb.Repositories
@using VectorWeb.Services
@using System.Text.RegularExpressions
@using System.Globalization
@using System.Timers
@inject NumeracionRangoService RangoService
@inject IRepository<CatTipoDocumento> RepoTipos
@inject IRepository<CatOficina> RepoOficinas
@inject IJSRuntime JS
@implements IDisposable
@rendermode InteractiveServer

<div class="d-flex justify-content-between align-items-center mb-3">
    <h3><i class="bi bi-sliders"></i> Gestión de Rangos y Cupos</h3>
</div>

@* --- MENSAJES DE ALERTA (Sin Toast, integrados) --- *@
@if (!string.IsNullOrWhiteSpace(mensajeError))
{
    <div class="alert alert-danger alert-dismissible fade show" role="alert">
        <i class="bi bi-exclamation-triangle-fill me-2"></i>@mensajeError
        <button type="button" class="btn-close" @onclick="() => mensajeError = string.Empty"></button>
    </div>
}

@if (!string.IsNullOrWhiteSpace(mensajeOk))
{
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        <i class="bi bi-check-circle-fill me-2"></i>@mensajeOk
        <button type="button" class="btn-close" @onclick="() => mensajeOk = string.Empty"></button>
    </div>
}

@* --- SISTEMA DE PESTAÑAS --- *@
<ul class="nav nav-tabs mb-4">
    <li class="nav-item">
        <button class="nav-link @(tabActiva == TabConfig.Rangos ? "active fw-bold border-primary border-bottom-0" : "")"
                @onclick="() => CambiarTab(TabConfig.Rangos)">
            <i class="bi bi-list-ol me-2"></i>Rangos
            <span class="badge bg-secondary rounded-pill ms-2">@listaRangos.Count</span>
        </button>
    </li>
    <li class="nav-item">
        <button class="nav-link @(tabActiva == TabConfig.Cupos ? "active fw-bold border-success border-bottom-0 text-success" : "text-success")"
                @onclick="() => CambiarTab(TabConfig.Cupos)">
            <i class="bi bi-pie-chart me-2"></i>Cupos
            <span class="badge bg-success bg-opacity-75 rounded-pill ms-2">@libroMayorCupos.Count</span>
        </button>
    </li>
    <li class="nav-item">
        <button class="nav-link @(tabActiva == TabConfig.Bitacora ? "active fw-bold border-warning border-bottom-0 text-dark" : "text-muted")"
                @onclick="() => CambiarTab(TabConfig.Bitacora)">
            <i class="bi bi-clock-history me-2"></i>Bitácora
        </button>
    </li>
</ul>

@* --- CONTENIDO DE LAS PESTAÑAS --- *@

@if (procesando && cargandoInicial)
{
    <div class="text-center p-5">
        <div class="spinner-border text-primary" role="status"></div>
        <p class="text-muted mt-2">Cargando configuración...</p>
    </div>
}
else
{
    /* =================================================================================
       PESTAÑA 1: RANGOS DE NUMERACIÓN
       ================================================================================= */
    @if (tabActiva == TabConfig.Rangos)
    {
        <div class="card shadow-sm border-0 animate__animated animate__fadeIn">
            <div class="card-body">
                <div class="row g-2 align-items-center mb-4">
                    <div class="col-md-4">
                        <button class="btn btn-primary w-100" @onclick="AbrirNuevoRango" disabled="@procesando">
                            <i class="bi bi-plus-lg me-2"></i> Nuevo Rango
                        </button>
                    </div>
                    <div class="col-md-8">
                        <div class="input-group">
                            <span class="input-group-text bg-light border-end-0"><i class="bi bi-search"></i></span>
                            <input type="text" class="form-control border-start-0 ps-0"
                                   placeholder="Buscar por tipo, oficina, año o nombre de serie..."
                                   @bind="filtroTexto" @bind:event="oninput" />
                            @if (!string.IsNullOrEmpty(filtroTexto))
                            {
                                <button class="btn btn-outline-secondary" @onclick="() => filtroTexto = string.Empty" title="Limpiar filtro">
                                    <i class="bi bi-x-lg"></i>
                                </button>
                            }
                        </div>
                    </div>
                </div>

                <div class="table-responsive">
                    <table class="table table-hover align-middle mb-0">
                        <thead class="table-light">
                            <tr>
                                <th>Tipo</th>
                                <th>Oficina</th>
                                <th class="text-center">Año</th>
                                <th>Serie (Nombre)</th>
                                <th class="text-end">Rango</th>
                                <th class="text-end">Último Uso</th>
                                <th class="text-center">Estado</th>
                                <th class="text-end" style="min-width: 200px;">Acciones</th>
                            </tr>
                        </thead>
                        <tbody>
                            @if (!RangosFiltrados.Any())
                            {
                                <tr><td colspan="8" class="text-center py-5 text-muted">No se encontraron rangos que coincidan.</td></tr>
                            }
                            else
                            {
                                @foreach (var rango in RangosFiltrados)
                                {
                                    <tr>
                                        <td>
                                            <span class="fw-bold text-primary">@(rango.IdTipoNavigation?.Nombre ?? $"Tipo {rango.IdTipo}")</span>
                                        </td>
                                        <td>
                                            @if (rango.IdOficinaNavigation != null)
                                            {
                                                <span class="badge bg-info text-dark"><i class="bi bi-building me-1"></i>@rango.IdOficinaNavigation.Nombre</span>
                                            }
                                            else
                                            {
                                                <span class="badge bg-secondary"><i class="bi bi-exclamation-circle me-1"></i>Sin oficina</span>
                                            }
                                        </td>
                                        <td class="text-center">@rango.Anio</td>
                                        <td><code>@rango.NombreRango</code></td>
                                        <td class="text-end">
                                            <span class="badge bg-light text-dark border">@rango.NumeroInicio</span>
                                            <i class="bi bi-arrow-right-short text-muted"></i>
                                            <span class="badge bg-light text-dark border">@rango.NumeroFin</span>
                                        </td>
                                        <td class="text-end fw-bold">@rango.UltimoUtilizado</td>
                                        <td class="text-center">
                                            @if (rango.Activo)
                                            {
                                                <span class="badge bg-success-subtle text-success-emphasis border border-success-subtle">
                                                    <i class="bi bi-check-circle-fill me-1"></i>Activo
                                                </span>
                                            }
                                            else
                                            {
                                                <span class="badge bg-secondary-subtle text-secondary-emphasis border border-secondary-subtle">
                                                    <i class="bi bi-dash-circle me-1"></i>Inactivo
                                                </span>
                                            }
                                        </td>
                                        <td class="text-end">
                                            <button class="btn btn-sm btn-outline-primary me-1" @onclick="() => Editar(rango)" title="Editar">
                                                <i class="bi bi-pencil me-1"></i>Editar
                                            </button>
                                            <button class="btn btn-sm btn-outline-danger" @onclick="() => EliminarRango(rango)" title="Eliminar">
                                                <i class="bi bi-trash me-1"></i>Eliminar
                                            </button>
                                        </td>
                                    </tr>
                                }
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    }

    /* =================================================================================
       PESTAÑA 2: CUPOS (LIBRO MAYOR) - MEJORADA
       ================================================================================= */
    @if (tabActiva == TabConfig.Cupos)
    {
        <div class="card shadow-sm border-0 animate__animated animate__fadeIn">
            <div class="card-body">
                <div class="d-flex justify-content-between align-items-center mb-4">
                    <h5 class="card-title mb-0 text-success">Libro Mayor de Cupos</h5>
                    <div>
                        <button class="btn btn-outline-secondary me-2" @onclick="CargarLibroMayorCuposAsync" title="Recargar">
                            <i class="bi bi-arrow-clockwise me-1"></i>Recargar
                        </button>
                        <button class="btn btn-success" @onclick="AbrirNuevoCupo">
                            <i class="bi bi-plus-circle me-2"></i> Asignar Nuevo Cupo
                        </button>
                    </div>
                </div>

                <div class="table-responsive">
                    <table class="table table-hover align-middle mb-0">
                        <thead class="table-light">
                            <tr>
                                <th>Tipo de Documento</th>
                                <th class="text-center">Año</th>
                                <th class="text-end">Cupo Total</th>
                                <th class="text-end">Consumido</th>
                                <th class="text-end" style="width: 30%;">Disponibilidad</th>
                            </tr>
                        </thead>
                        <tbody>
                            @if (libroMayorCupos.Count == 0)
                            {
                                <tr><td colspan="5" class="text-center py-4 text-muted">No hay cupos asignados aún.</td></tr>
                            }
                            else
                            {
                                @foreach (var item in libroMayorCupos)
                                {
                                    var porcentaje = item.Cantidad > 0 ? (double)item.Consumido / item.Cantidad * 100 : 0;
                                    // MEJORA: Lógica de semáforo más estricta
                                    var claseBarra = porcentaje > 95 ? "bg-danger animate__animated animate__pulse animate__infinite"
                                    : (porcentaje > 80 ? "bg-warning"
                                    : "bg-success");
                                    var claseTexto = porcentaje > 95 ? "text-danger fw-bold"
                                    : (porcentaje > 80 ? "text-warning fw-bold"
                                    : "text-muted");

                                    <tr>
                                        <td class="fw-bold text-dark">@item.Tipo</td>
                                        <td class="text-center">@item.Anio</td>
                                        <td class="text-end fw-bold">@item.Cantidad</td>
                                        <td class="text-end">@item.Consumido</td>
                                        <td>
                                            <div class="d-flex flex-column">
                                                <div class="progress" style="height: 10px;">
                                                    <div class="progress-bar @claseBarra" role="progressbar" style="width: @porcentaje%"></div>
                                                </div>
                                                <div class="d-flex justify-content-between mt-1 small">
                                                    <span>@porcentaje.ToString("F1")% usado</span>
                                                    <span class="@claseTexto">@item.Disponible disp.</span>
                                                </div>
                                            </div>
                                        </td>
                                    </tr>
                                }
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    }

    /* =================================================================================
       PESTAÑA 3: BITÁCORA
       ================================================================================= */
    @if (tabActiva == TabConfig.Bitacora)
    {
        <div class="card shadow-sm border-0 animate__animated animate__fadeIn">
            <div class="card-body">
                <div class="d-flex justify-content-between align-items-center mb-3">
                    <h5 class="card-title mb-0">Historial de Eventos</h5>
                    <button type="button" class="btn btn-sm btn-outline-secondary" @onclick="RecargarBitacora">
                        <i class="bi bi-arrow-clockwise me-1"></i> Actualizar
                    </button>
                </div>

                <div class="table-responsive">
                    <table class="table table-sm table-striped table-hover mb-0" style="font-size: 0.9rem;">
                        <thead class="table-light">
                            <tr>
                                <th>Fecha</th>
                                <th>Acción</th>
                                <th>Detalle</th>
                            </tr>
                        </thead>
                        <tbody>
                            @if (bitacora.Count == 0)
                            {
                                <tr><td colspan="3" class="text-center py-3 text-muted">Sin eventos registrados.</td></tr>
                            }
                            else
                            {
                                @foreach (var evento in bitacora)
                                {
                                    <tr>
                                        <td class="text-nowrap" style="width: 150px;">@evento.Fecha.ToString("dd/MM/yyyy HH:mm")</td>
                                        <td style="width: 120px;">
                                            <span class="badge w-100 @ObtenerColorAccion(evento.Accion)">@evento.Accion</span>
                                        </td>
                                        <td>@evento.Detalle</td>
                                    </tr>
                                }
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    }
}

@* --- MODAL RANGO (EDICIÓN/NUEVO) --- *@
@if (mostrarModal)
{
    <div class="modal fade show d-block" tabindex="-1" style="background-color: rgba(0,0,0,0.5);">
        <div class="modal-dialog modal-lg modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header bg-primary text-white">
                    <h5 class="modal-title">@(rangoActual.IdRango == 0 ? "Nuevo Rango" : "Editar Rango")</h5>
                    <button type="button" class="btn-close btn-close-white" @onclick="CerrarModal"></button>
                </div>
                <div class="modal-body">
                    @if (!string.IsNullOrWhiteSpace(mensajeErrorModalRango))
                    {
                        <div class="alert alert-danger" role="alert">@mensajeErrorModalRango</div>
                    }

                    <EditForm Model="rangoActual" OnSubmit="IgnorarSubmitRango">
                        <DataAnnotationsValidator />

                        <div class="row g-3">
                            <div class="col-md-6">
                                <label class="form-label fw-bold">Tipo de Documento</label>
                                <InputSelect @bind-Value="rangoActual.IdTipo" class="form-select" @onchange="OnParametrosSugerenciaRangoCambio">
                                    <option value="0">-- Seleccione --</option>
                                    @foreach (var tipo in tipos)
                                    {
                                        <option value="@tipo.IdTipo">@tipo.Nombre</option>
                                    }
                                </InputSelect>
                            </div>

                            @* --- BUSCADOR DE OFICINA CON DEBOUNCE --- *@
                            <div class="col-md-6">
                                <label class="form-label fw-bold">Oficina</label>
                                <div class="position-relative">
                                    <div class="input-group">
                                        <span class="input-group-text bg-light"><i class="bi bi-search"></i></span>
                                        <input type="text"
                                               class="form-control"
                                               placeholder="Escriba para buscar oficina..."
                                               value="@oficinaBusquedaRango"
                                               @oninput="OnBusquedaOficinaInput"
                                               @onfocus="MostrarSugerenciasOficinaRango"
                                               @onkeydown="ManejarTecladoOficinasRango" />
                                        @* MEJORA: Usamos oninput para el debounce *@
                                    </div>

                                    @if (oficinasRangoFiltradas.Any())
                                    {
                                        <div class="list-group position-absolute w-100 shadow-sm z-3" style="max-height: 220px; overflow-y: auto;">
                                            @for (var i = 0; i < oficinasRangoFiltradas.Count; i++)
                                            {
                                                var oficina = oficinasRangoFiltradas[i];
                                                var claseActivo = i == indiceOficinaRangoSeleccionada ? "active" : string.Empty;
                                                <button type="button" class="list-group-item list-group-item-action @claseActivo" @onclick="() => SeleccionarOficinaRango(oficina)">
                                                    @oficina.Nombre
                                                </button>
                                            }
                                        </div>
                                    }
                                </div>
                                <div class="form-text text-muted">@MensajeOficinaRango</div>
                            </div>

                            <div class="col-md-4">
                                <label class="form-label fw-bold">Año</label>
                                <InputNumber @bind-Value="rangoActual.Anio" class="form-control" @oninput="OnParametrosSugerenciaRangoCambio" />
                            </div>
                            <div class="col-md-8">
                                <label class="form-label text-muted">Nombre de Serie (Auto-generado)</label>
                                <input type="text" class="form-control bg-light" value="@rangoActual.NombreRango" readonly />
                            </div>

                            <div class="col-md-3">
                                <label class="form-label fw-bold">Desde</label>
                                <InputNumber @bind-Value="rangoActual.NumeroInicio" class="form-control" />
                            </div>
                            <div class="col-md-3">
                                <label class="form-label fw-bold">Hasta</label>
                                <InputNumber @bind-Value="rangoActual.NumeroFin" class="form-control" />
                            </div>
                            <div class="col-md-3">
                                <label class="form-label fw-bold">Cantidad a Asignar</label>
                                <input type="number"
                                       class="form-control"
                                       min="0"
                                       value="@cantidadSugeridaSolicitada"
                                       @oninput="OnCantidadSugeridaInput" />
                                <div class="form-text">Usada para sugerencia en tiempo real.</div>
                            </div>
                            <div class="col-md-3">
                                <label class="form-label fw-bold">Último Utilizado</label>
                                <InputNumber @bind-Value="rangoActual.UltimoUtilizado" class="form-control" />
                            </div>

                            @if (rangoActual.IdTipo > 0 && rangoActual.Anio > 0)
                            {
                                <div class="col-12">
                                    <div class="alert alert-info border mb-0">
                                        <div class="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-2">
                                            <h6 class="mb-0 fw-bold"><i class="bi bi-lightbulb me-1"></i>Sugerencia de asignación</h6>
                                            <button type="button" class="btn btn-sm btn-outline-primary" @onclick="AplicarSugerenciaRango" disabled="@(!PuedeAplicarSugerencia)">
                                                <i class="bi bi-magic me-1"></i>Aplicar sugerencia
                                            </button>
                                        </div>

                                        @if (!string.IsNullOrWhiteSpace(mensajeErrorSugerenciaRango))
                                        {
                                            <div class="text-danger small mb-2">@mensajeErrorSugerenciaRango</div>
                                        }

                                        <div class="row g-2 small">
                                            <div class="col-md-3"><span class="text-muted">Cupo total:</span> <strong>@sugerenciaRango.CupoTotal.ToString("N0", CulturaUi)</strong></div>
                                            <div class="col-md-3"><span class="text-muted">Asignado:</span> <strong>@sugerenciaRango.ConsumoTotalAsignado.ToString("N0", CulturaUi)</strong></div>
                                            <div class="col-md-3 text-success"><span class="text-muted">Disponible:</span> <strong>@sugerenciaRango.SaldoDisponible.ToString("N0", CulturaUi)</strong></div>
                                        </div>

                                        <hr class="my-2" />

                                        <div class="row g-2 small">
                                            <div class="col-md-4"><span class="text-muted">Inicio sugerido:</span> <strong>@sugerenciaRango.NumeroInicioSugerido.ToString("N0", CulturaUi)</strong></div>
                                            <div class="col-md-4"><span class="text-muted">Fin sugerido:</span> <strong>@sugerenciaRango.NumeroFinSugerido.ToString("N0", CulturaUi)</strong></div>
                                            <div class="col-md-4"><span class="text-muted">Cantidad:</span> <strong>@sugerenciaRango.CantidadSugerida.ToString("N0", CulturaUi)</strong></div>
                                        </div>
                                        @* Avisos varios de lógica de negocio *@
                                        @if (MostrarAvisoExisteRangoActivo)
                                        {
                                            <div class="small text-primary mt-2"><i class="bi bi-info-circle me-1"></i>Oficina ya tiene rango activo.</div>
                                        }
                                        @if (sugerenciaRango.SugerenciaRecortadaPorSaldo)
                                        {
                                            <div class="small text-warning mt-2"><i class="bi bi-exclamation-triangle me-1"></i>Ajustado por saldo disponible.</div>
                                        }
                                    </div>
                                </div>
                            }
                        </div>

                        <div class="mt-4 form-check form-switch">
                            <InputCheckbox @bind-Value="rangoActual.Activo" class="form-check-input" id="checkActivo" />
                            <label class="form-check-label" for="checkActivo">Rango Activo para consumo</label>
                        </div>

                        <div class="modal-footer px-0 pb-0 mt-3">
                            <button type="button" class="btn btn-secondary" @onclick="CerrarModal">Cancelar</button>
                            <button type="button" class="btn btn-primary" @onclick="Guardar" disabled="@procesando">
                                @if (procesando)
                                {
                                    <span class="spinner-border spinner-border-sm me-1"></span>
                                }
                                Guardar
                            </button>
                        </div>
                    </EditForm>
                </div>
            </div>
        </div>
    </div>
}

@* --- MODAL CUPO --- *@
@if (mostrarModalCupo)
{
    <div class="modal fade show d-block" tabindex="-1" style="background-color: rgba(0,0,0,0.5);">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header bg-success text-white">
                    <h5 class="modal-title">Configurar Cupo Anual</h5>
                    <button type="button" class="btn-close btn-close-white" @onclick="CerrarModalCupo"></button>
                </div>
                <div class="modal-body">
                    @if (!string.IsNullOrWhiteSpace(mensajeErrorModalCupo))
                    {
                        <div class="alert alert-danger" role="alert">@mensajeErrorModalCupo</div>
                    }

                    <div class="mb-3">
                        <label class="form-label fw-bold">Tipo de Documento</label>
                        <InputSelect @bind-Value="cupoTipoSeleccionado" class="form-select" @oninput="OnCupoFiltroCambio">
                            <option value="0">-- Seleccione --</option>
                            @foreach (var tipo in tipos)
                            {
                                <option value="@tipo.IdTipo">@tipo.Nombre</option>
                            }
                        </InputSelect>
                    </div>
                    <div class="mb-3">
                        <label class="form-label fw-bold">Año</label>
                        <InputNumber @bind-Value="cupoAnio" class="form-control" />
                    </div>
                    <div class="mb-3">
                        <label class="form-label fw-bold">Cantidad Total (Cupo)</label>
                        <InputNumber @bind-Value="cupoCantidad" class="form-control form-control-lg" />
                    </div>

                    @if (cupoTipoSeleccionado > 0)
                    {
                        <div class="alert alert-light border">
                            <div class="d-flex justify-content-between">
                                <span>Consumido actualmente:</span>
                                <strong>@cupoConsumido</strong>
                            </div>
                            <div class="d-flex justify-content-between mt-1 text-success">
                                <span>Disponible si guarda:</span>
                                <strong>@(Math.Max(0, cupoCantidad - cupoConsumido))</strong>
                            </div>
                        </div>
                    }

                    <div class="modal-footer px-0 pb-0">
                        <button type="button" class="btn btn-secondary" @onclick="CerrarModalCupo">Cancelar</button>
                        <button type="button" class="btn btn-success" @onclick="GuardarCupo" disabled="@procesando">
                            @if (procesando)
                            {
                                <span class="spinner-border spinner-border-sm me-1"></span>
                            }
                            Guardar Cupo
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>
}

@code {
    // --- VARIABLES DE ESTADO ---
    private enum TabConfig { Rangos, Cupos, Bitacora }
    private TabConfig tabActiva = TabConfig.Rangos;
    private Timer _debounceTimer; // Timer para el debounce

    private List<MaeNumeracionRango> listaRangos = new();
    private List<CatTipoDocumento> tipos = new();
    private List<CatOficina> oficinas = new();
    private MaeNumeracionRango rangoActual = new();
    private List<MaeNumeracionBitacora> bitacora = new();
    private List<CupoLibroMayorItem> libroMayorCupos = new();

    private bool mostrarModal;
    private bool mostrarModalCupo;
    private bool procesando = false;
    private bool cargandoInicial = true;

    // Mensajes
    private string mensajeError = string.Empty;
    private string mensajeOk = string.Empty;
    private string mensajeErrorModalRango = string.Empty;
    private string mensajeErrorModalCupo = string.Empty;

    // Filtros
    private string filtroTexto = string.Empty;

    // Búsqueda Oficina (Rango)
    private string oficinaBusquedaRango = string.Empty;
    private List<CatOficina> oficinasRangoFiltradas = new();
    private int indiceOficinaRangoSeleccionada = -1;
    private bool _busquedaOficinaPendiente = false;

    // Cupo
    private int cupoTipoSeleccionado;
    private int cupoAnio = DateTime.Now.Year;
    private int cupoCantidad;
    private int cupoConsumido;

    // Sugerencias
    private int? cantidadSugeridaSolicitada;
    private SugerenciaRangoNumeracion sugerenciaRango = new();
    private string mensajeErrorSugerenciaRango = string.Empty;
    private static readonly CultureInfo CulturaUi = CultureInfo.GetCultureInfo("es-DO");

    // Lógica de filtrado en memoria para la tabla de rangos
    private IEnumerable<MaeNumeracionRango> RangosFiltrados =>
        string.IsNullOrWhiteSpace(filtroTexto)
        ? listaRangos
        : listaRangos.Where(r =>
            (r.NombreRango?.Contains(filtroTexto, StringComparison.OrdinalIgnoreCase) ?? false) ||
            (r.IdTipoNavigation?.Nombre?.Contains(filtroTexto, StringComparison.OrdinalIgnoreCase) ?? false) ||
            CoincideOficinaSinImportarOrden(r.IdOficinaNavigation?.Nombre, filtroTexto)
          );

    // --- INICIALIZACIÓN Y DISPOSE ---
    protected override async Task OnInitializedAsync()
    {
        // Configurar Debounce (300ms espera)
        _debounceTimer = new Timer(300);
        _debounceTimer.AutoReset = false;
        _debounceTimer.Elapsed += async (sender, e) =>
        {
            await InvokeAsync(async () =>
            {
                if (_busquedaOficinaPendiente)
                {
                    await EjecutarBusquedaOficina();
                    _busquedaOficinaPendiente = false;
                }

                await RefrescarSugerenciaRangoAsync();
                StateHasChanged();
            });
        };

        try
        {
            await CargarCatalogos();
            await CargarTodo();
        }
        finally
        {
            cargandoInicial = false;
        }
    }

    public void Dispose()
    {
        _debounceTimer?.Dispose();
    }

    // --- LÓGICA DE NAVEGACIÓN ---
    private void CambiarTab(TabConfig tab)
    {
        tabActiva = tab;
        LimpiarMensajes();
    }

    private void LimpiarMensajes()
    {
        mensajeError = string.Empty;
        mensajeOk = string.Empty;
        mensajeErrorModalRango = string.Empty;
        mensajeErrorModalCupo = string.Empty;
    }

    // --- CARGA DE DATOS ---
    private async Task CargarTodo()
    {
        procesando = true;
        try
        {
            await CargarRangos();
            await CargarLibroMayorCuposAsync();
            await RecargarBitacora();
        }
        finally
        {
            procesando = false;
        }
    }

    private async Task CargarCatalogos()
    {
        tipos = (await RepoTipos.GetAllAsync()).OrderBy(t => t.Nombre).ToList();
        oficinas = (await RepoOficinas.GetAllAsync()).OrderBy(o => o.Nombre).ToList();
    }

    private async Task CargarRangos() => listaRangos = await RangoService.ObtenerRangosAsync();
    private async Task CargarLibroMayorCuposAsync() => libroMayorCupos = await RangoService.ObtenerLibroMayorCuposAsync();
    private async Task RecargarBitacora() => bitacora = await RangoService.ObtenerBitacoraAsync();

    // --- GESTIÓN DE RANGOS ---
    private void AbrirNuevoRango()
    {
        LimpiarMensajes();
        rangoActual = new MaeNumeracionRango
        {
            NombreRango = "AUTO",
            NumeroInicio = 1,
            NumeroFin = 1000,
            UltimoUtilizado = 0,
            Activo = true,
            Anio = DateTime.Now.Year
        };
        ResetearBusquedaOficina();
        cantidadSugeridaSolicitada = 1000;
        sugerenciaRango = new SugerenciaRangoNumeracion();
        mostrarModal = true;
        _ = RefrescarSugerenciaRangoAsync();
    }

    private void Editar(MaeNumeracionRango rango)
    {
        LimpiarMensajes();
        rangoActual = new MaeNumeracionRango
        {
            IdRango = rango.IdRango,
            IdTipo = rango.IdTipo,
            NombreRango = rango.NombreRango,
            NumeroInicio = rango.NumeroInicio,
            NumeroFin = rango.NumeroFin,
            UltimoUtilizado = rango.UltimoUtilizado,
            Activo = rango.Activo,
            IdOficina = rango.IdOficina,
            Anio = rango.Anio
        };
        oficinaBusquedaRango = rango.IdOficinaNavigation?.Nombre ?? string.Empty;
        oficinasRangoFiltradas.Clear();
        indiceOficinaRangoSeleccionada = -1;
        cantidadSugeridaSolicitada = rangoActual.NumeroFin - rangoActual.NumeroInicio + 1;
        mostrarModal = true;
        _ = RefrescarSugerenciaRangoAsync();
    }

    private async Task Guardar()
    {
        mensajeErrorModalRango = string.Empty;
        procesando = true;
        try
        {
            if (rangoActual.IdTipo <= 0) { mensajeErrorModalRango = "Seleccione un tipo."; return; }
            if (!rangoActual.IdOficina.HasValue) { mensajeErrorModalRango = "Seleccione una oficina."; return; }
            if (rangoActual.NumeroInicio > rangoActual.NumeroFin) { mensajeErrorModalRango = "Rango inválido (Inicio > Fin)."; return; }

            // IMPORTANTE: Aquí se espera que el servicio valide si hay otro rango activo y devuelva Fail si es necesario.
            var resultado = await RangoService.GuardarRangoAsync(rangoActual);

            if (!resultado.Exitoso)
            {
                mensajeErrorModalRango = resultado.Mensaje;
                return;
            }

            mostrarModal = false;
            mensajeOk = "Rango guardado correctamente.";
            await CargarTodo();
        }
        finally { procesando = false; }
    }

    private async Task EliminarRango(MaeNumeracionRango rango)
    {
        bool confirmado = await JS.InvokeAsync<bool>("confirm", $"¿Está seguro de eliminar el rango '{rango.NombreRango}'?");
        if (!confirmado) return;

        procesando = true;
        LimpiarMensajes();
        try
        {
            var resultado = await RangoService.EliminarRangoAsync(rango.IdRango);
            if (!resultado.Exitoso) { mensajeError = resultado.Mensaje; return; }

            mensajeOk = "Rango eliminado correctamente.";
            await CargarTodo();
        }
        finally { procesando = false; }
    }

    // --- LÓGICA DE BÚSQUEDA DE OFICINA (DEBOUNCE) ---
    private void OnBusquedaOficinaInput(ChangeEventArgs e)
    {
        oficinaBusquedaRango = e.Value?.ToString() ?? string.Empty;
        _busquedaOficinaPendiente = true;
        // Reiniciamos el timer cada vez que el usuario escribe
        _debounceTimer.Stop();
        _debounceTimer.Start();
    }

    private void OnCantidadSugeridaInput(ChangeEventArgs e)
    {
        if (int.TryParse(e.Value?.ToString(), out int valor))
        {
            cantidadSugeridaSolicitada = valor;
            _busquedaOficinaPendiente = false;
            _debounceTimer.Stop();
            _debounceTimer.Start();
        }
        else if (string.IsNullOrWhiteSpace(e.Value?.ToString()))
        {
            cantidadSugeridaSolicitada = null;
            _busquedaOficinaPendiente = false;
            _debounceTimer.Stop();
            _debounceTimer.Start();
        }
    }

    private async Task EjecutarBusquedaOficina()
    {
        rangoActual.IdOficina = null;
        indiceOficinaRangoSeleccionada = -1;

        if (string.IsNullOrWhiteSpace(oficinaBusquedaRango))
        {
            oficinasRangoFiltradas.Clear();
        }
        else
        {
            oficinasRangoFiltradas = BuscarOficinasRango(oficinaBusquedaRango).Take(8).ToList();
            // Si hay coincidencia exacta, auto-seleccionar ID (opcional)
            var exacta = oficinas.FirstOrDefault(o => string.Equals(o.Nombre, oficinaBusquedaRango, StringComparison.OrdinalIgnoreCase));
            if (exacta != null) rangoActual.IdOficina = exacta.IdOficina;
        }
    }

    private void ResetearBusquedaOficina()
    {
        oficinaBusquedaRango = string.Empty;
        oficinasRangoFiltradas.Clear();
        indiceOficinaRangoSeleccionada = -1;
    }

    private async Task SeleccionarOficinaRango(CatOficina oficina)
    {
        rangoActual.IdOficina = oficina.IdOficina;
        oficinaBusquedaRango = oficina.Nombre ?? string.Empty;
        oficinasRangoFiltradas.Clear();
        indiceOficinaRangoSeleccionada = -1;
        await RefrescarSugerenciaRangoAsync();
    }

    private void MostrarSugerenciasOficinaRango()
    {
        if (!string.IsNullOrWhiteSpace(oficinaBusquedaRango) && !oficinasRangoFiltradas.Any())
            _debounceTimer.Start(); // Forzar búsqueda si hace foco y hay texto
    }

    private async Task ManejarTecladoOficinasRango(KeyboardEventArgs e)
    {
        if (!oficinasRangoFiltradas.Any()) return;
        if (e.Key == "ArrowDown") { indiceOficinaRangoSeleccionada = Math.Min(indiceOficinaRangoSeleccionada + 1, oficinasRangoFiltradas.Count - 1); return; }
        if (e.Key == "ArrowUp") { indiceOficinaRangoSeleccionada = Math.Max(indiceOficinaRangoSeleccionada - 1, 0); return; }
        if (e.Key == "Enter" && indiceOficinaRangoSeleccionada >= 0) { await SeleccionarOficinaRango(oficinasRangoFiltradas[indiceOficinaRangoSeleccionada]); return; }
        if (e.Key == "Escape") { oficinasRangoFiltradas.Clear(); indiceOficinaRangoSeleccionada = -1; }
    }

    // --- SUGERENCIAS ---
    private async Task RefrescarSugerenciaRangoAsync()
    {
        mensajeErrorSugerenciaRango = string.Empty;
        if (rangoActual.IdTipo <= 0 || rangoActual.Anio <= 0)
        {
            sugerenciaRango = new SugerenciaRangoNumeracion();
            return;
        }

        sugerenciaRango = await RangoService.ObtenerSugerenciaRangoAsync(
            rangoActual.IdTipo, rangoActual.Anio, rangoActual.IdOficina,
            rangoActual.IdRango == 0 ? null : rangoActual.IdRango, cantidadSugeridaSolicitada);

        if (sugerenciaRango.CupoTotal == 0)
            mensajeErrorSugerenciaRango = "No existe cupo configurado para este tipo y año.";

        StateHasChanged(); // Asegurar refresco UI si viene de un evento async
    }

    private async Task OnParametrosSugerenciaRangoCambio(ChangeEventArgs _)
    {
        await RefrescarSugerenciaRangoAsync();
    }

    private async Task AplicarSugerenciaRango()
    {
        await RefrescarSugerenciaRangoAsync();
        if (!PuedeAplicarSugerencia) return;
        rangoActual.NumeroInicio = sugerenciaRango.NumeroInicioSugerido;
        rangoActual.NumeroFin = sugerenciaRango.NumeroFinSugerido;
    }

    // --- GESTIÓN DE CUPOS ---
    private void AbrirNuevoCupo()
    {
        LimpiarMensajes();
        cupoTipoSeleccionado = 0;
        cupoCantidad = 0;
        cupoConsumido = 0;
        mostrarModalCupo = true;
    }

    private async Task GuardarCupo()
    {
        mensajeErrorModalCupo = string.Empty;
        procesando = true;
        try
        {
            if (cupoTipoSeleccionado <= 0) { mensajeErrorModalCupo = "Seleccione un tipo."; return; }
            if (cupoAnio < 2000) { mensajeErrorModalCupo = "Año inválido."; return; }

            var resultado = await RangoService.GuardarCupoAsync(cupoTipoSeleccionado, cupoAnio, cupoCantidad);
            if (!resultado.Exitoso) { mensajeErrorModalCupo = resultado.Mensaje; return; }

            mensajeOk = "Cupo guardado correctamente.";
            mostrarModalCupo = false;
            await CargarTodo();
        }
        finally { procesando = false; }
    }

    private async Task OnCupoFiltroCambio(ChangeEventArgs e)
    {
        if (e.Value is string val && int.TryParse(val, out int id))
        {
            cupoTipoSeleccionado = id;
            if (id > 0)
            {
                cupoCantidad = await RangoService.ObtenerCantidadCupoAsync(id, cupoAnio) ?? 0;
                cupoConsumido = await RangoService.ObtenerConsumoAcumuladoAsync(id, cupoAnio);
            }
        }
    }

    // --- UTILIDADES ---
    private void CerrarModal() { mostrarModal = false; oficinasRangoFiltradas.Clear(); }
    private void CerrarModalCupo() => mostrarModalCupo = false;
    private Task IgnorarSubmitRango(EditContext _) => Task.CompletedTask;

    private IEnumerable<CatOficina> BuscarOficinasRango(string terminoBusqueda)
    {
        var palabras = terminoBusqueda.Split(' ', StringSplitOptions.RemoveEmptyEntries | StringSplitOptions.TrimEntries);
        if (palabras.Length == 0) return Enumerable.Empty<CatOficina>();
        return oficinas.Where(oficina => palabras.All(palabra => (oficina.Nombre?.Contains(palabra, StringComparison.OrdinalIgnoreCase) ?? false))).OrderBy(oficina => oficina.Nombre);
    }

    private static bool CoincideOficinaSinImportarOrden(string? nombreOficina, string? busqueda)
    {
        if (string.IsNullOrWhiteSpace(nombreOficina) || string.IsNullOrWhiteSpace(busqueda)) return false;
        var tokensBusqueda = Regex.Split(busqueda.Trim(), @"\s+").Where(token => !string.IsNullOrWhiteSpace(token));
        return tokensBusqueda.All(token => nombreOficina.Contains(token, StringComparison.OrdinalIgnoreCase));
    }

    private string MensajeOficinaRango => rangoActual.IdOficina is not null
        ? "Oficina seleccionada correctamente."
        : "Seleccione una oficina específica.";

    private bool PuedeAplicarSugerencia => sugerenciaRango.CantidadSugerida > 0 && sugerenciaRango.SaldoDisponible > 0;
    private bool MostrarAvisoExisteRangoActivo => sugerenciaRango.OficinaTieneRangoActivo && !sugerenciaRango.RangoActivoAgotado;

    private string ObtenerColorAccion(string accion) => accion switch
    {
        "APERTURA" => "bg-success",
        "CIERRE" => "bg-secondary",
        "ELIMINACION" => "bg-danger",
        "CAMBIO" => "bg-warning text-dark",
        _ => "bg-info text-dark"
    };
}
